import { PagePanel } from "@/components/ui/page-panel";

export default function InventoryPage() {
  return (
    <PagePanel
      title="Inventory packages"
      description="Placeholder for package search, package detail, quantity status, adjustment history, and reconciliation flags. Full CRUD is intentionally deferred until the audit and fake Metrc foundations are verified."
    />
  );
}
