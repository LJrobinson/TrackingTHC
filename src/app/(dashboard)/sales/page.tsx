import { PagePanel } from "@/components/ui/page-panel";

export default function SalesPage() {
  return (
    <PagePanel
      title="Mock POS sales"
      description="Placeholder for the cart workflow, package quantity validation, mock tender capture, sale completion, inventory deduction, audit trail writes, and fake Metrc sales receipt sync."
    />
  );
}
